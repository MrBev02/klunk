# Build output. Nothing here is written by hand.
dist/
dist-single/
spike-dist-single/

# Generated by npm, enormous, and reformatting it would conflict on every
# install.
package-lock.json

# The spike is gitignored and deliberately rough (#3).
spike/
vite.spike.config.ts

# Markdown is prose, and this project's prose is hand-wrapped with aligned
# tables that Prettier would reflow into something harder to read. CLAUDE.md is
# the document the whole repository is explained in; churning it to satisfy a
# formatter would cost more than the tidiness is worth.
*.md
