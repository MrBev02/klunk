{
  "name": "klunk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Klunk builds exam papers from a question bank. Runs entirely in the browser, no server, no API keys.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "test:corpus": "vitest run --mode corpus",
    "test:watch": "vitest",
    "build": "tsc --noEmit && vitest run && vite build",
    "build:single": "tsc --noEmit && vite build --mode single",
    "build:all": "npm run build && npm run build:single",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "pdfjs-dist": "^6.2.108",
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "@types/node": "^26.1.2",
    "@types/wicg-file-system-access": "^2023.10.7",
    "prettier": "3.9.6",
    "typescript": "^5.6.0",
    "vite": "^8.1.5",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^4.1.10"
  }
}
